export interface SubscribeOptions {
    concurrency: number;
    execTimeout: number;
}
interface PubsubMessage {
    publishTime: string;
    data: string;
}
interface SubscribeFunction {
    (payload: {}, pmsg: PubsubMessage): Promise<any>;
}
export declare function subscribe(subName: string, execFunc: SubscribeFunction, opts?: SubscribeOptions): void;
export {};
