const { tryPublish } = require('../pubsub/publish');

let message = 0;
const TEST_TOPIC = 'ccart-dev/topics/gapi-lite-test';

setInterval(() => {
  tryPublish(TEST_TOPIC, { message })
  console.log('published ->> ', message);
  message++;
}, 2000);
